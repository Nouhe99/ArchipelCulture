using UnityEngine;
using UnityEngine.UI;

public class ChangeProfilPicture : MonoBehaviour
{
    //public Image image;
    //public ItemData dataProfil;

    //public void SelectThisPicture()
    //{
    //    if (Database.Instance.userData.profilPicture != dataProfil.ID)
    //    {
    //        Database.Instance.userData.profilPicture = dataProfil.ID;
    //        UIManager.current.profil.pictureProfil.sprite = image.sprite;
    //        UIManager.current.topBarPanel.avatarImage.sprite = image.sprite;
    //        UIManager.current.profil.pictureHasChanged = true;
    //        //if (Database.Instance.userData.id == "local")
    //        //    PlayerPrefs.SetString(PlayerPrefs.GetString("LocalConnectedKey") + "Picture", dataProfil.ID);
    //    }
    //    UIManager.current.profil.CloseActualTab();
    //}

    //public void FillInfos(ItemData data)
    //{
    //    image.sprite = data.Sprite;
    //    dataProfil = data;
    //    if(data.Rarity == null) return;
    //    if (data.Rarity.Case == null) //rarity background
    //        gameObject.GetComponent<Image>().color = Color.clear;
    //    else
    //    {
    //        Image temp = gameObject.GetComponent<Image>();
    //        temp.sprite = data.Rarity.Case;
    //        if (temp.color == Color.clear) temp.color = Color.white;
    //    }
    //}
}
